---
export const prerender = false;

// Obtener el slug completo de la URL
const fullSlug = Astro.params.slug;

// Redirigir directamente sin verificación
return Astro.redirect(`/post/${fullSlug}`, 301);
---