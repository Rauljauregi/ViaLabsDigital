---
export const prerender = false;

// Obtener y limpiar el slug
const slug = Astro.params.slug?.replace(/^\/+|\/+$/g, '');
console.log('Redirigiendo desde newsletter/post/', slug); // Para depuración

// Redirigir a la nueva ruta
return Astro.redirect(`/post/${slug}`, 301);
---