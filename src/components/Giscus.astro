---
import { siteConfig } from '@/data/site.config'
import Comments from './comments.jsx'
const giscusConfig = siteConfig.config.commentsProps
const commentsEnabled = siteConfig.config.commentsEnabled
---

<Comments client:only='react' enabled={Astro.props.enabled} {...giscusConfig} />
